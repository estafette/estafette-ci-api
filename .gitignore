# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# ignore build and helm artifacts
/estafette-ci-api
/estafette-ci-api-*.tgz
venv/
gcs-migrator/dist
gcs-migrator/build
.go/
.pyconfig/
