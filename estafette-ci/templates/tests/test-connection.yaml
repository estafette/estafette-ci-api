apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "estafette-ci.fullname" . }}-test-connection"
  labels:
{{ include "estafette-ci.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "estafette-ci.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
